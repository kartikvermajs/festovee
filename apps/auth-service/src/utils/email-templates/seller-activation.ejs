<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Verification</title>
    <style>
        /* Base Reset */
        body {
            margin: 0;
            padding: 0;
            background-color: #f6f9fc;
            font-family: "Segoe UI", Helvetica, Arial, sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        table {
            border-collapse: collapse;
            width: 100%;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
        }

        .header {
            padding: 30px 20px 10px;
            text-align: center;
            background: linear-gradient(135deg, #fdfdfd, #f6f9fc);
        }

        .header img {
            width: 90px;
            height: auto;
        }

        .title {
            text-align: center;
            font-size: 24px;
            font-weight: 700;
            margin: 20px 0 10px;
            color: #1a1a1a;
        }

        .subtitle {
            text-align: center;
            font-size: 16px;
            color: #555555;
            margin-bottom: 30px;
            padding: 0 20px;
        }

        .otp-wrapper {
            text-align: center;
            margin: 30px auto;
        }

        .otp {
            display: inline-block;
            background: #f2f5ff;
            color: #1a1a1a;
            padding: 18px 32px;
            font-size: 32px;
            font-weight: bold;
            border-radius: 12px;
            letter-spacing: 8px;
            text-align: center;
        }

        .expiry {
            text-align: center;
            font-size: 14px;
            color: #666666;
            margin: 20px 0 40px;
        }

        .footer {
            background-color: #fafafa;
            text-align: center;
            padding: 20px;
            font-size: 12px;
            color: #999999;
            border-top: 1px solid #eeeeee;
        }

        .footer p {
            margin: 5px 0;
        }

        /* Responsive Styles */
        @media (max-width: 600px) {
            .title {
                font-size: 20px;
            }

            .subtitle {
                font-size: 14px;
                padding: 0 15px;
            }

            .otp {
                font-size: 26px;
                padding: 15px 24px;
                letter-spacing: 6px;
            }
        }
    </style>
</head>

<body>
    <table role="presentation" width="100%">
        <tr>
            <td align="center" style="padding:20px;">
                <div class="container">

                    <!-- Header -->
                    <div class="header">
                        <img src="https://res.cloudinary.com/dh9j3przi/image/upload/v1758581393/favicon_gdlyeb.png"
                            alt="FESTOVEE Logo">
                    </div>

                    <!-- Title & Subtitle -->
                    <h1 class="title">Verify Your Email</h1>
                    <p class="subtitle">Thank you for registering with Festovee. To activate your seller account, Plese
                        use the following activation code.</p>

                    <!-- OTP -->
                    <div class="otp-wrapper">
                        <div class="otp">
                            <%= otp %>
                        </div>
                    </div>

                    <p class="expiry">This OTP will expire in <strong>5 minutes</strong>.</p>

                    <!-- Footer -->
                    <div class="footer">
                        <p>If you didn’t request this email, please ignore it.</p>
                        <p>&copy; <%= new Date().getFullYear() %> FESTOVEE. All rights reserved.</p>
                    </div>
                </div>
            </td>
        </tr>
    </table>
</body>

</html>